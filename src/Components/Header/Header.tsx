import * as React from 'react';

import './Header.css';

const Header = () => {
    return (
        <div id="header">
            <h1>City</h1>
        </div>
    );
};

export default Header;
