{
  "name": "control-air",
  "version": "1.0.0",
  "description": "Spécialiste en mesure et analyse des comportements aérauliques",
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-project/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-project/node_modules"
  ],
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "nodemailer": "^4.4.1",
    "request": "^2.83.0"
  }
}
