{
  "name": "test-react",
  "version": "1.0.0",
  "description": "How to use create-react-app with a custom Node API on Heroku",
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-project/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-project/node_modules"
  ],
  "dependencies": {
    "bootstrap": "^3.3.7",
    "express": "^4.16.2"
  }
}
